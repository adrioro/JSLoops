var MyArray = new Array("Adriana", "Monica", "Patricia");

for (v in MyArray) {
  document.write("Sudent: " + MyArray[v] + "<br>");
}

var SecondArray = (["9", "7", "5", "10"], ["Sonia", "John", "Adam", "Jorge"]);

for (d in SecondArray[i]) {
  document.write(SecondArray[i][d]);
}